const letterEmoji = document.getElementById('letter').addEventListener('click', handleLetterClicked);
const letter = "/img/letter.png";

function handleLetterClicked(event) {
    console.log(event.target);

}
